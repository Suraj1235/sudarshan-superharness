# Demo Build Specification

Build a small, production-quality Python package that exposes addition and subtraction functions.

Requirements:

- Type-annotate the public functions.
- Include unit tests for positive, negative, and zero values.
- Include a concise README with usage examples.
- Use only the Python standard library.
- Verification must run through `python -m unittest -q`.
